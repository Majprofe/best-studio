<section
  id="contact"
  class="text-white flex flex-col justify-center items-center py-16 bg-black md:scroll-mt-header"
  aria-labelledby="contact-title"
>
  <h2 id="contact-title" class="text-center text-4xl font-extrabold uppercase tracking-widest">
    Contacto
  </h2>
  <h3 class="text-light text-center pt-4 tracking-wide max-w-lg lg:text-xl">
    Escríbenos y pasa a formar parte de<br />
    <strong class="text-white">The King of Gyms</strong>
  </h3>

  <div
    class="text-white flex flex-col md:flex-row md:justify-evenly items-center p-10 gap-12"
  >
    <!-- Contáctanos -->
    <div class="card" aria-labelledby="contact-us">
      <div class="content">
        <h2 id="contact-us" class="text-2xl font-bold mb-4">Contáctanos</h2>
        <a
          href="mailto:info@beststudio.com"
          target="_blank"
          rel="noopener noreferrer"
          class="block text-lg mb-2 text-gray-300 hover:text-gray-100 transition-colors"
          >info@beststudio.com</a
        >
        <a
          href="tel:+34657710761"
          target="_blank"
          rel="noopener noreferrer"
          class="block text-lg text-gray-300 hover:text-gray-100 transition-colors"
          >657 71 07 61</a
        >
        <a
          href="https://api.whatsapp.com/send?phone=34657710761"
          target="_blank"
          rel="noopener noreferrer"
          class="block mt-4"
        >
          <img
            src="/whatsappIcon.svg"
            alt="WhatsApp Icon"
            class="inline-block w-12 h-12 object-contain bg-transparent hover:opacity-80 transition-opacity"
          />
        </a>
      </div>
    </div>

    <!-- Dirección -->
    <div class="card" aria-labelledby="address">
      <div class="content">
        <h2 id="address" class="text-2xl font-bold mb-4">Dirección</h2>
        <p class="text-lg text-gray-300">C/ Curtidores, nave 16, 23740</p>
        <h3 class="text-lg text-gray-300">Andújar</h3>
        <a
          href="https://maps.app.goo.gl/N1u3ckTseNmeT1KP8"
          target="_blank"
          rel="noopener noreferrer"
          class="text-white flex justify-center items-center mt-4 hover:text-gray-100 transition-colors"
        >
          <img src="/location.svg" alt="Location Icon" class="h-6 w-auto pr-2" /> VER
          MAPA
        </a>
      </div>
    </div>

    <!-- Horarios -->
    <div class="card" aria-labelledby="hours">
      <div class="content">
        <h3 id="hours" class="text-2xl font-bold mb-4">Horarios</h3>
        <p class="text-lg text-gray-300">
          L-V: 08:00h a 15:00h y 16:00h a 22:00h
        </p>
        <p class="text-lg text-gray-300">S: 11:00h a 13:00h</p>
      </div>
    </div>
  </div>
</section>

<style>
/* Contenedor principal */
.card {
  background-color: #1a1a1a; /* Fondo oscuro */
  border-radius: 15px; /* Bordes redondeados */
  padding: 20px;
  width: 300px; /* Tamaño fijo */
  height: 250px; /* Tamaño fijo */
  text-align: center;
  display: flex;
  flex-direction: column;
  justify-content: center;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

/* Sombra y escalado en hover */
.card:hover {
  transform: translateY(-5px);
  box-shadow: 0 8px 20px rgba(255, 255, 255, 0.1); /* Sombra suave */
}

/* Título */
.content h2, .content h3 {
  color: #ffffff;
}

/* Texto del contenido */
.content p, .content a {
  color: #c0c0c0;
}

/* Efecto hover para enlaces */
.content a:hover {
  color: #ffffff; /* Blanco en hover */
  text-decoration: underline; /* Subrayado en hover */
}

/* Icono de WhatsApp */
.content img {
  transition: opacity 0.3s ease;
}

.content img:hover {
  opacity: 0.8; /* Efecto en hover */
}

/* Uniformidad en flexbox */
.text-white .card {
  flex: 1 1 auto; /* Asegura uniformidad en tamaños */
}
</style>
