<section
  id="contact"
  class="text-white flex flex-col justify-center items-center py-16 bg-black"
  aria-labelledby="contact-title"
>
  <h1 id="contact-title" class="text-center text-4xl font-extrabold uppercase tracking-widest">
    Contacto
  </h1>
  <h2 class="text-base text-center pt-4 tracking-wide max-w-lg lg:text-xl">
    Escríbenos y pasa a formar parte de<br />
    <strong class="text-tertiary">The King of Gyms</strong>
  </h2>

  <div
    class="text-white flex flex-col md:flex-row md:justify-evenly items-center p-10 gap-12"
  >
    <!-- Contáctanos -->
    <div class="card" aria-labelledby="contact-us">
      <div class="content">
        <h2 id="contact-us" class="text-2xl font-bold mb-4">Contáctanos</h2>
        <a
          href="mailto:info@beststudio.com"
          target="_blank"
          rel="noopener"
          class="block text-lg mb-2 text-gray-300 hover:text-tertiary transition-colors"
          >info@beststudio.com</a
        >
        <a
          href="tel:+34657710761"
          class="block text-lg text-gray-300 hover:text-tertiary transition-colors"
          >657 71 07 61</a
        >
        <a
          href="https://api.whatsapp.com/send?phone=34657710761"
          class="block mt-4"
        >
          <img
            src="/whatsappIcon.svg"
            alt="WhatsApp Icon"
            class="inline-block w-12 h-12 object-contain bg-transparent"
          />
        </a>
      </div>
    </div>

    <!-- Dirección -->
    <div class="card" aria-labelledby="address">
      <div class="content">
        <h2 id="address" class="text-2xl font-bold mb-4">Dirección</h2>
        <p class="text-lg text-gray-300">C/ Curtidores, nave 16, 23740</p>
        <h3 class="text-lg text-gray-300">Andújar</h3>
        <a
          href="https://maps.app.goo.gl/N1u3ckTseNmeT1KP8"
          rel="noopener"
          target="_blank"
          class="text-tertiary flex justify-center items-center mt-4 hover:text-white transition-colors"
        >
          <img src="/location.svg" alt="Location Icon" class="h-6 w-auto pr-2" /> VER
          MAPA
        </a>
      </div>
    </div>

    <!-- Horarios -->
    <div class="card" aria-labelledby="hours">
      <div class="content">
        <h3 id="hours" class="text-2xl font-bold mb-4">Horarios</h3>
        <p class="text-lg text-gray-300">
          L-V: 08:00h a 15:00h y 16:00h a 22:00h
        </p>
        <p class="text-lg text-gray-300">S: 11:00h a 13:00h</p>
      </div>
    </div>
  </div>
</section>

<style>
/* Estilos de la tarjeta sin borde visible, pero con borde animado y partido */
.card {
  position: relative;
  width: 300px;
  height: 250px;
  background-color: transparent; /* Fondo transparente para el borde animado */
  display: flex;
  justify-content: center;
  align-items: center;
  overflow: hidden;
}

.content {
  color: white;
  text-align: center;
  background-color: #151515;
  border-radius: 15px; /* Mantén el borde redondeado para el contenido */
  padding: 20px;
  width: 99%;
  height: 99%;
  position: relative;
  z-index: 1; /* Asegura que el contenido esté sobre el borde animado */
}

/* Pseudo-elemento para crear el borde animado partido */
.card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(90deg, #F4F91D 50%, #a7b621 50%);
  background-size: 200% 200%;
  border-radius: 15px; /* Borde animado con radio */
  z-index: 0;
  animation: smoothRotate 16s linear infinite;
}

/* Animación de rotación suave del borde animado */
@keyframes smoothRotate {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}

/* Efecto hover de la tarjeta */
.card:hover {
  transform: scale(1.05);
  box-shadow: 0 0 20px rgba(244, 249, 29, 0.7); /* Sombra ligera en hover */
}



</style>