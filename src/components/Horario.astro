<section id="horario" class="horario-container scroll-mt-header">
    <a href="https://beststudio.aimharder.com/timetable" target="_blank" rel="noopener noreferrer" class="horario-link">
        <h2 class="horario-title">HORARIOS</h2>
    </a>
</section>

<style>
    /* Contenedor */
    .horario-container {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 2rem;
        text-align: center;
    }

    /* Enlace estilo botón */
    .horario-link {
        display: inline-block;
        padding: 1rem 2rem;
        border-radius: 0.5rem;
        background-color: rgba(224, 224, 224, 0.1); /* Fondo suave */
        border: 2px solid rgba(224, 224, 224, 0.4); /* Borde sutil */
        text-decoration: none;
        transition: background-color 0.3s ease, box-shadow 0.3s ease;
    }

    .horario-link:hover {
        background-color: rgba(224, 224, 224, 0.2); /* Fondo más intenso en hover */
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3); /* Sombra más pronunciada */
    }

    /* Estilo del título */
    .horario-title {
        font-size: 3rem; /* Tamaño inicial */
        font-weight: bold;
        background: linear-gradient(90deg, #e0e0e0, #ffffff, #b3b3b3, #e0e0e0); /* Gradiente suave */
        background-size: 200%;
        background-clip: text;
        -webkit-background-clip: text;
        color: transparent; /* Necesario para gradiente en texto */
        animation: gradient-shift 6s infinite linear; /* Desplazamiento del gradiente */
    }

    @media (min-width: 768px) {
        .horario-title {
            font-size: 4rem; /* Tamaño más grande en pantallas mayores */
        }
    }

    /* Animación del gradiente */
    @keyframes gradient-shift {
        0% {
            background-position: 200% 0;
        }
        100% {
            background-position: -200% 0;
        }
    }
</style>
